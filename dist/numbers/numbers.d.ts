export declare type Format = 'percentage' | 'currency';
export declare function formatNumber(num: number, format?: Format): string;
export declare function toString(num: number, numberOfDecimals?: number): string;
